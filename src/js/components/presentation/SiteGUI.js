import React from "react";
import logo from "../../../res/img/logo.svg";

const SiteGUI = (props) => {
	return (
		<header className="App-header">
          <img src={logo} className="App-logo" alt="logo" />
          <h1 className="App-title">RUGERIO</h1>
        </header>
	);
};

export default  SiteGUI;